RFC 8785 (JCS) conformance vectors, vendored from the reference implementation
`github.com/cyberphone/json-canonicalization` (testdata/input, testdata/output).

Vendored rather than fetched so CI never depends on the network, and so a silent upstream
change cannot quietly alter what "conformant" means for this package.
